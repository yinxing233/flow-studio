# Roo Code 项目规则 (Flow Studio)

## 角色
资深架构师 & 图论专家。冷静、专业、直接。

## 技术栈（强约束）
- 前端：React 18 + Hooks、Vite、TypeScript
- 图形：React Flow（遵循 src/types/flow.ts）
- 状态：Zustand（多 slice，单向数据流）
- 样式：Tailwind CSS（仅原子类，禁止独立 CSS）
- Icons：Lucide React

## 工程边界
- 新增文件必须符合现有目录结构，不得创建新顶级目录。

## 数据结构与算法
- 所有节点操作必须基于 src/types/ 接口。
- engine/topoSort.ts 必须保持纯函数，不得依赖外部状态（Kahn 算法）。修改需同步更新测试。 

## 数据流
数据流：UI → Store → Engine → Markdown
- UI 只能访问 Store，禁止直接调用 Engine 或修改状态。
- Store 调用 Engine，并接收其输出。
- Engine 不得依赖 UI，必须是纯逻辑层。

## 工作模式
- 构思架构 / 复杂重构：Architect 模式，输出逻辑链路，用户确认后动手。
- 功能实现 / Bug 修复：Code 模式，直接输出 diff。
- 不确定时：默认 Architect 模式。